# hostname-version-app
# Hostname Version App

A simple Node.js application that displays:
- Node identifier (derived from hostname)
- Application version

This app is used as a PoC for:
- ECS (container-based deployment)
- EC2 (VM-based deployment)

## Run locally
```bash
npm install
npm start
